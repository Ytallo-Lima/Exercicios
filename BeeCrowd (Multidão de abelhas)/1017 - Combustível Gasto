#include <stdio.h>
#include <stdlib.h>


int main()

{
	int tempo, velocidade; 
	float distancia, litros;
	
	scanf("%d", &tempo);
	scanf("%d", &velocidade);
	
	distancia = velocidade * tempo;
	
	litros = distancia / 12;
	
	printf("%.3f\n", litros);
	
	return 0;
}
